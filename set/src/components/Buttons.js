import React from 'react';

const Buttons = (props) => {
  return (
    <div>
      <button>NO SET</button>
      <button>PAUSE</button>
      <button>RESTART</button>
    </div>
  )
}

export default Button;